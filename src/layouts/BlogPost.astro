---
import type { CollectionEntry } from "astro:content";
import BaseHead from "../components/BaseHead.astro";
import FormattedDate from "../components/FormattedDate.astro";
import { randomizeTagColors } from "../utils/tag";
import { Icon } from "astro-icon/components";
import Layout from "./Layout.astro";

interface Props {
  // Ambil semua data dari skema Zod
  post: CollectionEntry<"blog">["data"];
  // Tambahkan property minutesRead secara manual di sini
  minutesRead: string;
  lastModified: string;
}

const { title, tags, pubDate, updatedDate, description } = Astro.props.post;
const { minutesRead, lastModified } = Astro.props;
---

<html>
  <head>
    <BaseHead
      title={title}
      description={description.join(". ")}
      image={`https://www.readmecodegen.com/api/og-generator?title=${encodeURIComponent(title)}&subtitle=${encodeURIComponent(description.join(". "))}&author=Putra&template=cleanModernArticle&pattern=tech&appName=putracoderz.my.id`}
    />
  </head>

  <body>
    <Layout title={title} description={description.join(". ")}>
      <main class="max-w-3/5 mx-auto prose my-3">
        <a role="button" class="btn btn-ghost btn-lg" href="/blog">
          <Icon name="material-symbols:arrow-back" />
          Back to Blog
        </a>
        <div class="">
          <div class="">
            <div>
              <p>{minutesRead}</p>
              <div class="date">
                <FormattedDate date={pubDate} />
                {
                  updatedDate && (
                    <div class="last-updated-on">
                      Last updated on <FormattedDate date={updatedDate} />
                    </div>
                  )
                }
              </div>
              <h1>{title}</h1>
              {description.map((d) => <p>{d}</p>)}
              <div class="flex gap-1 my-2">
                {tags.map((t) => <div class={`badge badge-soft`}>{t}</div>)}
              </div>
              <p class="text-end">Last Edited {lastModified}</p>
              <hr />
            </div>
          </div>
        </div>
        <div>
          <slot />
        </div>
        <!-- <Footer /> -->
      </main>
    </Layout>
  </body>
</html>
